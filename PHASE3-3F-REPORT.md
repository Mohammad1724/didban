# فاز ۳ — گام 3-F: ممیزی تم تیره (Cockpit) + RTL + کاهش حرکت (Reduce-Motion)

تاریخ: ۲۰۲۶-۰۹-۱۴

## ۱. نتیجهٔ کلیدی

ساختار تم از 3-A درست بود (token-based، بدون سایه، با کانال `DidbanTheme` برای صفحات جاسازی‌شده)؛ اما ممیزی سه‌محوره **۹ نقطهٔ ایراد** یافت که ۴ تای آن‌ها در زبان پیش‌فرض اپلیکیشن (فارسی/RTL) رفتار نادرست می‌داشتند — مهم‌ترینش‌ها این بود که **کل نقشهٔ فضایی در fa آینه‌وار برعکس می‌شد** (مختصات مطلق از `start=` می‌گذشت). همهٔ موارد رفع شد.

## ۲. محور RTL — یافته‌ها و اصلاحات

| # | ایراد | پیامد | اصلاح |
|---|---|---|---|
| R1 | اوریالای‌های نقشه (tap-target و لیبل هر نود) با `padding(start=...)` جابه‌جا می‌شدند | در fa (پیش‌فرض) کل نقشه در محور X آینه می‌شد؛ هدف اشاره به نودِ مخالف می‌خورد | 5 موقعیت به `left=` (مختصات مطلق = مستقل از جهت) |
| R2 | منوی شعاعی (3 دکمه: test/refresh/cockpit) همان الگوی `start=` | منوی long-press در fa جابه‌جا می‌شد | `left=` در 3 موقعیت |
| R3 | فلش «ایران → خارج» در fleet (`Icons.Rounded.ArrowForward`) | در fa جهت جریان داده برعکس دیده می‌شد (ایران راست، فلش به راست!) | 2 فلش به `Icons.AutoMirrored.Rounded.ArrowForward` — در fa آینه می‌شود، در en دست‌نخورده |
| R4 | `DenseCellText` سلول‌های غیر-LTR را **فرضاً** Rtl می‌کرد | در لوکال en کل جدول‌ها RTL می‌شدند | وراثت `LocalLayoutDirection.current` (fa→Rtl، en→Ltr) |
| R5 | هم‌راستایی LTR در `DenseCellText`/`v3ListHeaderRow` هم‌یشگی `End` | در en ستون‌های IP/متن به راست می‌چسبیدند | `End` فقط وقتی پایهٔ جدول RTL باشد (هم‌راستایی لبهٔ دیداری، مطابق `DenseCellText`) |

**بررسی‌شده و بدون ایراد:** فلش‌های بازگشت از قبل `AutoMirrored` بودند؛ نوار HUD و نقطه‌های صفحه‌نما از `start/end` و `weight` جهت‌آگاه استفاده می‌کنند؛ نوار لغزشی (Pager) به‌صورت ذاتی جهت چیدمان را رعایت می‌کند (در RTL جلو بردن صفحات با سوییپ به راست — رفتار صحیح پلتفرم).

## ۳. محور تم تیره (Cockpit) — یافته‌ها و اصلاحات

- **معماری تم از قبل درست بود:** `AeroTheme` → `DidbanTheme(dark = resolved.isDark, palette = …)`؛ یعنی همهٔ صفحات قدیمیِ جاسازی‌شده (Servers، Network/Cloud، Vault، Settings) خودکار با تم v3 همراه می‌شوند — با grep امضاها و نقطهٔ اتصال تأیید شد.
- **21 لایهٔ `copy(alpha=…)` در فایل‌های v3** — همه روی توکن‌های `Ds.*`؛ هیچ رنگ خامی در آن‌ها نیست.
- **هیچ shadow/elevation در v3** — تیره‌ایِ تم تیره سالم است (مخصوصاً طراحی شده).
- **یک رنگ سخت‌کدشده** در gliph onboarding (`Color(0xFF14161C)`) — که حتی با کالاس واقعی Cockpit (`0xFF070B14`) نمی‌خواند → حالا `Color(AeroCockpitTokens.canvas)` (نماد، دقیقاً همان رنگ واقعی تم را نشان می‌دهد).
- **به‌عناد و باقی‌مانده:** scrim پالت (`Color.Black` ۳۵٪) — scrim مدال استاندارد در هر دو تم؛ تغییر نمی‌کند. `CrashRecoveryScreen` با `dark=true` رفتار قدیمی‌اش را نگه می‌دارد (خارج از محدودهٔ v3).

## ۴. محور کاهش حرکت (Reduce-Motion) — یافته‌ها و اصلاحات

هوک سیستمی (`Settings.Secure: accessibility_reduce_motion`) از 3-A وجود داشت اما فقط **۲ از ۸** جای متحرک آن را رعایت می‌کردند (درخشش اسکلتون و جریان خط‌چین نقشه). حالا:

- **کمکی مشترک جدید:** `aeroTween(ms)` در `AeroTheme.kt` — وقتی کاهش‌حرکت روشن است، `tween(0)` (لحظه‌ای) برمی‌گرداند؛ هر انیمیشن v3 با یک خط آن را رعایت می‌کند.
- **۶ جای متحرک دیگر سیم‌کشی شدند:**

| مکان | رفتار جدید با کاهش‌حرکت |
|---|---|
| نوار بخش‌های کاکپیت (6 چیپ + onNavigate) | پرش لحظه‌ای به‌جای سوییپ (`scrollToPage`) |
| PulseDot کانتینرهای Docker | نقطه می‌ماند ولی پالس نمی‌کند |
| دک‌های ناوبری پالت (4 فرمان صفحه) | پرش لحظه‌ای |
| fade راهنمای سوییپ (deckHint) | ظاهر/غیب لحظه‌ای |
| باز/بسته‌شدن جزئیات fleet | لحظه‌ای |
| تعویض صفحه onboarding | لحظه‌ای |

- **تغییرنما (سندشده):** دو حلقهٔ `while(true)` در کاکپیت و fleet **پولینگ داده**‌اند (تاریخچه/تست latency) نه انیمیشن — دست‌نخورده.

## ۵. فهرست تغییرات

| فایل | تغییر |
|---|---|
| `AeroTheme.kt` | +`aeroTween(ms)`؛ +۲ ایمپورت؛ R4/R5 در `DenseCellText` و `v3ListHeaderRow` |
| `AeroCanvasHome.kt` | R1/R2: 5 موقعیت `start=` → `left=`؛ 4 فرمان ناوبری reduce-motion-aware؛ deckHint با `aeroTween` |
| `AeroCockpit.kt` | +`reduceMotion`؛ `gotoPage()` مشترک (چیپ‌ها + onNavigate)؛ PulseDot با کاهش‌حرکت خاموش می‌شود |
| `AeroTunnelFleet.kt` | R3: 2 فلش AutoMirrored (با تعویض ایمپورت)؛ AnimatedVisibility با `aeroTween` |
| `AeroOnboarding.kt` | رنگ gliph = توکن واقعی Cockpit؛ AnimatedContent با `aeroTween`؛ ایمپورت `tween` زائد حذف |

موتور (Go/agent)، قراردادها و صفحات فاز ۲: بدون تغییر.

## ۶. شواهد تست

| ردیف | نتیجه |
|---|---|
| کامپایل بستهٔ JVM (19 فایل) | MAIN OK |
| تست‌های JVM | OK (126 tests) |
| `go build ./...` (agent) | OK |
| `go test -race -count=1` | ok (1.932s) |

**نکتهٔ صداقت:** همهٔ فایل‌های تغییرکردهٔ این گام لایهٔ Compose هستند (خارج از بستهٔ JVM)؛ ماتریس بالا تأیید می‌کند هیچ پس‌رفت به موتور و مدل‌ها نیفتاده، اما خودِ تغییرات Compose — به دلیل نبود SDK اندروید در این محیط — امکان تست واقعی نداشت و فقط از نظر کد بررسی شد (تعادل براکت، ایمپورت‌ها، قراردادهای `Padding.left`/`AutoMirrored`/`tween(0)`/`scrollToPage`، و بازبینی نقطه‌به‌نقطهٔ هر موقعیت).

**معروف‌شده (خارج از محدوده):** `AeroCockpit.kt` دو ایمپورت زائد قدیمی (`TextAlign`، `Dp`) از زمان 3-C دارد — بی‌اثر بر اجرا؛ عمداً دست‌نخورده ماند تا این گام فقط همان سه محور را بگذارد.

## ۷. وضعیت فاز ۳

**3-A → 3-B → 3-C → 3-D → 3-E → 3-F: هر شش گام تحویل‌شده و push شده.** طرح v3 «Aerospatial» کامل شد: نقشهٔ فضایی، دک ۵صفحه‌ای، کاکپیت، شیت‌های سیار، منوی شعاعی، پالت فرمان، onboarding یک‌بار، تم دوجانبه با پیش‌فرض روشن، و رعایت RTL/کاهش‌حرکت در سراسر سطوح.
