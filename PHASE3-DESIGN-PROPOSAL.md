# فاز ۳ — پروپوزال طراحی v3: «بوم فضایی» (Aerospatial)

> وضعیت: **پیش‌نویس برای تأیید** — هنوز کد پیاده‌سازی نوشته نشده است.
> تاریخ: ۲۰۲۶-09-14 · بستر: main @ a866dd6 (فاز ۲ کامل) · تم پیش‌فرض: **روشن پلاتین** (تصمیم کاربر)
>
> **یادداشت فرآیند (چرا v3):**
> - v1 (نگه‌داشت ۵ تب + کارت‌ها + cyan) رد شد: «شبیه الان است».
> - v2 (میز فرمان: نقشه + HUD + command bar) رد شد: **«ساختار هنوز شبیه فعلی است»**.
> - تشخیص ریشه‌ای: در v2 محتوای کارت‌ها عوض شده بود ولی **اسکلت** (نوار بالا + استک کارت + نوار پایین) همان اسکلت اپ فعلی بود.
> - v3 اسلکت را عوض می‌کند. مقایسهٔ ساختاری در بخش ۲.

---

## ۱. مفهوم

**اپ = یک بوم فضایی تمام‌بها (full-bleed).** صفحهٔ خانه یک نقشهٔ زندهٔ توبولوژی است که لبه تا لبهٔ نمایشگر را پر کرده — نه یک کارت درون یک صفحه. هیچ نوار اپلیکیشن بالایی، هیچ درک پایین، هیچ لیست کارتی وجود ندارد.

### پنج لایه (به‌جای «صفحه‌ها»)

| لایه | چیست | جایگزینِ چیست در فعلی |
|---|---|---|
| **۰ — بوم (خانه)** | نقشهٔ توبولوژی تمام‌بها + یک خوشهٔ HUD شناور یک‌خطی بالای صفحه | ۵ تب + لیست کارت سرورها + NetworkCloud |
| **۱ — دکِ افقی** | swipe افقی روی بوم: ۴ صفحهٔ تمام‌بها (نقشه / تانل‌ها / آپ‌تایم / ابزارها) با نقاط pagination | ناوبری تبی عمودی |
| **۲ — برگهٔ زمینه‌ای** | ضربه روی نود → کوکپیت سرور **به‌صورت sheet محکم‌شده** از پایین؛ ضربه روی کمان → برگهٔ تانل. بوم پشتش **محوشده ولی دیده می‌شود** — کانتکست هرگز گم نمی‌شود | DashboardScreen و هر صفحهٔ جزئیات (nav stack) |
| **۳ — منوی شعاعی** | نگه‌داشتن (long-press) روی سرور/بوم → اکشن‌های زمینه‌ای در حلقهٔ شعاعی دور نقطهٔ لمس (SSH، SFTP، پینگ، Systemd، Benchmark، Vault) | درک پایین + اکشن‌های منو |
| **۴ — پالت فرمان** | swipe-up یا چیپ «دستور» در HUD → overlay جست‌وجوی همه‌چیز: سرورها/تانل‌ها/ابزارها/تنظیمات + recents | تب VaultTools + هر دسترسی پنهان |

## ۲. مقایسهٔ ساختاری با اپ فعلی (این است که عوض می‌شود)

| فعلی | v3 |
|---|---|
| `TopAppBar` + محتوا + `LiquidSpotlightDock` (۵ تب) | **هیچ** نوار اپلیکیشن؛ یک HUD شناور یک‌خطی (خوشهٔ ابزار) |
| ناوبری: ۵ حالت `currentNav` + `openServer` | ناوبری: **فضایی** (بوم → sheet/radial/palette) + دکِ افقی |
| هر صفحهٔ جزئیات = صفحهٔ کامل جدید (nav stack) | جزئیات = **sheet** روی بوم؛ بوم همیشه کانتکست است |
| لیست کارت (هر آیتم = کارت) | **جداول متراکم با hairline** (فرایندها، تانل‌ها) + **نودهای فضایی** (سرورها) |
| ردیف ۴ کارت KPI | **نوار ابزارِ پیوسته** (یک خوشه، سلول‌های جداشده با hairline — مثل کوکپیت واقعی) |
| ابزارها در تب ۵ + پخش در منوها | **پالت فرمان** (یک ورودی، همهٔ ابزارها) + منوی شعاعی زمینه‌ای |

## ۳. دو تم (پیش‌فرض: روشن)

| | **پلاتین (روشن — پیش‌فرض)** | **کوکپیت (تیره — اختیاری)** |
|---|---|---|
| هویت | کاغذ سرد، جوهر سرمه‌ای، primary navy | اتاق فرمان شب، amber |
| canvas | `#F4F6FA` | `#070B14` |
| surface | `#FFFFFF` / low `#EEF2F8` | `#0D1421` / low `#090E18` |
| hairline | `#E3E8F0` / strong `#CBD5E1` | `#1B2536` / `#27364E` |
| متن | `#0E1B2C` / `#46566E` / `#8A97AB` | `#EEF2F8` / `#8FA0B8` / `#55647C` |
| primary | navy `#1E40AF` | amber `#FFB454` (deep `#C77E1B`) |
| ok/warn/danger | `#0E9F6E` / `#B45309` / `#DC2626` | `#2DD4A7` / `#F5B942` / `#FF5C5C` |
| info/violet/offline | `#2563EB` / `#7C3AED` / `#A8B2C2` | `#5CA8FF` / `#A78BFA` / `#3A465C` |

همان توکن‌ها، همان IA؛ فقط مقادیر عوض می‌شوند (`Ds.theme` — کد موازی ندارد).

## ۴. زبان بصری

- **تمام‌بها**: بوم بدون حاشیه؛ sheet با radius بالایی 30 و grabber؛ HUD pill شناور.
- **خوشهٔ ابزار (instrument band)**: یک سطح پیوسته، ۴ سلول با hairline عمودی جداکننده؛ هر سلول = micro-label + عدد بزرگ (tabular-nums) + mini-ring/delta.
- **جدول متراکم**: surface واحد + hairline بین ردیف‌ها + header کم‌رنگ؛ دادهٔ فنی mono-LTR.
- **نقشه**: نود = هسته + هالهٔ radial + رینگ وضعیت + label؛ کمان = بیزی با dash-flow متحرک (جهت)؛ قطع = خط‌چین قرمز ساکن.
- **motion**: sheet/radial/palette ≤220ms ease-out؛ dash-flow 1.6s (با `reduceMotion` ساکن)؛ pulse نود فقط هنگام تغییر وضعیت.
- **اعداد**: فارسی در نمایش؛ فنی (IP/پورت/PID/tokun) LTR + mono.
- **یک primary در هر context**؛ destructive فقط داخل confirm dialog.

## ۵. نگاشت ساختار فعلی → جدید (هیچ ویژگی حذف نمی‌شود)

| فعلی | جدید |
|---|---|
| تب سرورها (لیست) | نودهای بوم + برگهٔ کوکپیت |
| تب تانل‌ها | کمان‌های بوم + صفحهٔ ۲ِ دک (لیست متراکم) + برگهٔ تانل |
| تب Uptime | صفحهٔ ۳ِ دک + سلول SLA در HUD + برگهٔ جزئیات |
| تب NetworkCloud | **خود بوم** |
| تب VaultTools | پالت فرمان (گروه «ابزارها») + منوی شعاعی |
| DashboardScreen | برگهٔ کوکپیت |
| SshTerminal/Sftp/Systemd/Batch/Benchmark/Probe/Security | صفحه‌های عملکردی همانند قبل؛ ورود از پالت/شعاعی/rail |
| onboarding اولین سرور | صفحهٔ خالیِ بوم: «نقشه‌ات خالی است» + CTA افزودن سرور (با wizard همان فعلی) |

## ۶. سیستم ۴ حالتی (روی ساختار جدید)

- **خالی**: بوم با یک نودِ هُل + CTA «افزودن اولین سرور».
- **لود**: نودهای skeleton (پالس خاکستری) + HUD با `—`.
- **خطا**: banner شناور بالای بوم + «تلاش دوباره» (بوم با آخرین دادهٔ معتقد).
- **آفلاین-صادق**: نود خاکستری + «آخرین دیدار X دقیقه پیش»؛ داده‌های برگه با watermark «قدیمی» و هرگز به‌صورت زنده.

## ۷. mockup‌های این بسته (همهٔ اولیه: تم روشن)

| فایل | محتوا |
|---|---|
| `mockup-01-home-canvas.html` | بوم تمام‌بها + HUD + دک + (قاب ۲) منوی شعاعی |
| `mockup-02-cockpit-sheet.html` | کوکپیت سرور = برگهٔ زمینه‌ای روی بومِ محوشده |
| `mockup-03-tunnel-and-palette.html` | برگهٔ تانل با حالت‌های فاز ۲ (کشف‌شده/توکن) + پالت فرمان |
| `mockup-04-dark-theme.html` | همان دو حالت اصلی در تم کوکپیت تیره |

## ۸. plan پیاده‌سازی (کامیت‌های جدا + تست؛ قاعدهٔ فاز ۲)

| آیتم | محتوا | ریسک |
|---|---|---|
| **3-A** | `Ds` v3: دو تم (پلاتین پیش‌فرض + کوکپیت) + سوییچ تم + تایپو + پرایمیتیوهای UI: instrument band، جدول متراکم، ۴ حالت، motion/reduce | کم |
| **3-B** | **بوم**: نقشهٔ Canvas تمام‌بها (نود/هاله/کمان/flow) + HUD + swipe افقی دک (۴ صفحه) + بازنویسی shell MainActivity (حذف ۵ تب/دِرک) | **زیاد** (قلب اپ) |
| **3-C** | **برگهٔ کوکپیت** (محکم‌شده روی بوم؛ expand/collapse؛ chart ۲۴h؛ جدول فرایندها؛ rail) — جایگزین DashboardScreen | متوسط |
| **3-D** | **برگهٔ تانل** (همهٔ حالت‌های فاز ۲) + **پالت فرمان** (جست‌وجو + گروه‌ها + recents) + منوی شعاعی | متوسط |
| **3-E** | اتصال صفحه‌های ابزار از پالت/شعاعی + onboarding خالی + parity-check کامل (همهٔ قابلیت‌های فعلی قابل دسترسی) | متوسط |
| **3-F** | audit تم تیره + RTL + reduce-motion + matrix (JVM 54 + Go race) + گزارش نهایی | کم |

هر آیتم: build JVM (بخش‌های قابل کامپایل) + test matrix + بررسی کد Compose فقط (بدون emulator — صادقانه ذکر می‌شود).

## ۹. چیزهایی که عوض نمی‌شوند

- **موتور/Agent/کانتراکت API/امنیت**: دست نمی‌خورد (فاز ۲).
- **حالت‌های فاز ۲**: کشف‌شده، توکن ناشناسه، residue، allowlist env.
- **داده و فرکانس poll**: همان؛ انیمیشن‌ها صرفاً UI (درخواست شبکهٔ اضافه نمی‌آید).
- **صفحات عملکردی** (SSH/SFTP/…): منطقشان همان؛ فقط نقطهٔ ورود عوض می‌شود.

## ۱۰. صداقت دربارهٔ بستر و ریسک‌ها

- mockup‌ها HTML/CSS inline برای مرور در viewer؛ پیاده‌سازی Compose: بوم = `Canvas`، برگه‌ها = composable overlay با state (نه ModalBottomSheet خام — فیزیک sheet کنترل‌شده)، شعاعی = overlay با `Animatable`، پالت = overlay با `LazyColumn`.
- **ریسک درک‌پذیری long-press** در فارسی/دست‌های مختلف: در onboarding یک lesson کوتاه + چیپ «دستور» در HUD به‌عنوان مسیر جایگزین همیشه‌دسترس.
- **ریسک عملکردی** sheet+blur روی device کم‌قدرت: blur فقط هنگام باز بودن sheet و با `alphaLayer` سبک؛ در reduceMotion/lowRam خاموش می‌شود.

## ۱۱. سؤال تأیید

با **ساختار فضایی v3** (بوم = خانه، sheet به‌جای صفحه، شعاعی + پالت به‌جای تب‌ها، تم روشن پیش‌فرض) ادامه بدهیم؟ پس از تأیید → **3-A**.
