# گزارش آیتم ۲۸ — حذف placeholder توکن «auto-detected» (M17)

**فایل‌های تغییرکرده:** `TunnelModel.kt`، `TunnelSecrets.kt`، `TunnelEngine.kt`، `TunnelScreen.kt` + تست جدید `TunnelDiscoveryTest.kt`

## مشکل (ریشه)

`discoverTunnels` هر تانل کشف‌شده را با `token = "auto-detected"` ذخیره می‌کرد. این رشته
**placeholder بود ولی شبیه توکن واقعی رفتار می‌کرد**:

- `TunnelSecrets.ensureToken` فقط توکن **خالی** را می‌ساخت؛ `"auto-detected"` را به‌عنوان
  **توکن معتبر** برمی‌گرداند → در deploy بعدی، دقیقاً همین رشته در فایل تنظیم **هر دو**
  سمت (سرور و کلاینت) نوشته می‌شد.
- نتیجه: اگر فرآیند واقعیِ در حال اجرا توکن دیگری داشت، تانل deploy‌شده هرگز به آن
  وصل نمی‌شد — **شکست خاموش با اعتبارنامه‌ی جعلی** (و در بدترین حالت، اگر فرآیند واقعی
  همین توکن ضعیف را داشت، اعتبارنامه‌ی ضعیف به‌جای هشدار، تایید می‌شد).
- این با قواعد کیفیت تولید (بدون placeholder/داده‌ی جعلی/فیلد جادویی) هم ناسازگاری داشت.

## طراحی: «هرگز اعتبارنامه اختراع نکن؛ یا واقعی را بازیابی کن یا ناشناخته بگذار و block کن»

سه لایه:

### ۱. بازیابی واقعی (best-effort) — `TunnelSecrets.extractTokenFromCmd`
تنها هسته‌ای که توکنش را روی **خط دستور** می‌گذارد Chisel است (`--auth user:token`،
هر دو سمت). parser:
- فقط برای Chisel (بقیه null می‌دهند — توکنشان در فایل تنظیم است که اسکنر نمی‌خواند).
- مثل خود Chisel در **اولین** کالون user/pass را جدا می‌کند (پس‌ورود: کالون در
  password مجاز است).
- **نگهداری از truncation:** ایجنت cmdline را در ۱۰۰ کاراکتر بریده و `…` می‌چسباند؛
  اگر مقدار `--auth` آخرین توکن باشد و بریده شده، «توکن» در واقع پیشوندی ناقص است →
  رد می‌شود (null).
- خالی یا بیش از ۲۵۶ کاراکتر → null.

### ۲. درهای ورود placeholder بسته شدند
- اسکن پروس: `token = extractTokenFromCmd(core, proc.cmd) ?: ""` + `discovered = true`
  (از `proc.cmd`ِ بدون lowercase استخراج می‌شود — تشخیص‌ها روی نسخه‌ی lowercase است، ولی
  توکن باید case-همان‌نما بماند).
- اسکن Docker: `token = ""` + `discovered = true` (env کانتینر در API فعلی ایجنت
  نیست — عمداً حدس زده نمی‌شود).
- مدل: فیلد جدید `var discovered: Boolean = false` (+ ذخیره/بارگذاری JSON).
- **مهاجرت (fromJson):** هر رکورد قدیمی با `token == "auto-detected"` به
  `token = ""` + `discovered = true` التیام می‌یابد — داده‌های ذخیره‌شده‌ی نسخه‌های قبلی
  خودکار اصلاح می‌شوند.

### ۳. دروازه‌ی deploy (ریشه‌ی خطر) — `validateForDeploy`
```
اگر discovered && توکن‌دار هسته && توکن خالی → خطای روشن، deploy/کد‌سازی مسدود
```
- هسته‌های **بدون** توکن (GOST، IPTables) از دروازه معاف‌اند.
- چون `generateCode` هم از همین validate شروع می‌شود، دیالوگ «نمایش کد» هم پیام دلیل
  را نشان می‌دهد (مسیر H4) — نه کدی با توکن جعلی.

### ۴. UI (کوچک و هدفمند)
- **دیالوگ ویرایش:** برای تانل کشف‌شده با توکن خالی، دیگر توکن رندم پیش‌فرض **نمی‌شود**
  (که «ناشناخته» را بی‌صدا به «اختراعی» بدل کند) — خالی می‌ماند + هشدار زرد:
  «این تانل کشف‌شده است و توکن واقعی‌اش ناشناخته است…».
- **کارت تانل:** چیب کوچک «کشف‌شده» برای تانل‌های `discovered`.

## تست (JVM)

`TunnelDiscoveryTest.kt` — ۱۳ تست:
1. **استخراج:** server/client Chisel، quote، کالون داخل password (`admin:ab:cd` → `ab:cd` —
   همان رفتار خود Chisel)، بدون `--auth`، `--auth` بدون مقدار (حالت truncation)، هسته‌های
   دیگر (BackPack/GOST/FRP) → null، password خالی/بلند → null، truncation با `…` در آخر →
   null ولی اگر توکن بعدی `--auth` بعد از مقدار بیاید، مقدار سالم است.
2. **مهاجرت:** `auto-detected` → خالی + discovered؛ round-trip JSON؛ تانل دستی discovered نمی‌شود.
3. **دروازه:** هر ۸ هسته‌ی توکن‌دار + discovered + توکن خالی → `validateForDeploy` خطا +
   `generateCode` استثنا؛ GOST/IPTables discovered → عبور؛ discovered با توکن واقعی →
   کد تولیدشده **همان توکن واقعی** را دارد؛ تانل دستی با توکن خالی → توکن تازه ساخته
   می‌شود (رفتار کهنه حفظ).

**نتیجه:** `MAIN OK` + سوت کامل **`OK (49 tests)`**.

> شفافیت: تغییرات `TunnelScreen.kt` (Compose UI) در این سازه‌وار امکان کامپایل نداشت و
> فقط code-review شدند — همه‌ی آیدنتیفایرهای به‌کاررفته (`Ds.warn`، الگوی `Box`/`border`/
> `RoundedCornerShape`) از الگوهای موجود همان فایل برداشته شده‌اند. منطق اصلی (مدل،
> استخراج، دروازه) کاملاً JVM-test است.

## رفتار کاربردی (بعد از این آیتم)

| سناریو | قبل | بعد |
|---|---|---|
| کشف Chisel | توکن «auto-detected» | توکن واقعی (اگر در cmdline باشد) یا خالی |
| کشف بقیه هسته‌ها | توکن «auto-detected» | خالی + نشان «کشف‌شده» |
| deploy تانل کشف‌شده بدون توکن | **deploy با اعتبارنامه‌ی جعلی (خاموش)** | خطای روشن + راهنمای ورود توکن |
| ویرایش تانل کشف‌شده | توکن رندم بی‌صدا | خالی + هشدار؛ ورود دستی لازم است |
| رکوردهای قدیمی با placeholder | «auto-detected» می‌ماند | خودکار → خالی + discovered |

## باقی‌مانده (ثبت‌شده)

- **env کانتینرها** در API ایجنت موجود نیست (Docker scan) → توکن تانل‌های کشف‌شده از
  Docker (مثل Narnia با `PASSWORD`) همچنان «ناشناخته» می‌ماند و کاربر باید دستی وارد
  کند — رفتار امن و شفاف. افزودن env به endpoint docker (با `docker inspect`) کاندیدای
  پیگیری است.
- حدس `ir` در تشخیص ایران/خارج (M-grade) — بدون تغییر، ثبت‌شده.
