# گزارش فاز ۳ · آیتم 3-D — شیت تونل + پالت فرمان + منوی رادیال

تاریخ: ۲۰۲۶-۰۹-۱۴
وضعیت: **پایان‌یافته** — `TunnelScreen` جایگزین و حذف شد؛ لایه‌های Compose از نظر کد بررسی شده‌اند.

## ۱. لایه‌های ساخته‌شده (طبق مدل لایه‌ای v3)

### 3-D.1 — شیت تونل (لایه ۲ کانتکست) · `AeroTunnelFleet.kt` + `AeroTunnelForm.kt`
صفحه‌ی تونل دِک دیگر لیستِ کارت‌های قدیمی نیست؛ اکنون **دو سطح** دارد:

1. **سطح ناوگان (فلیت)** — یک `InstrumentBand` چهارسلولی (تعداد/فعال/AutoSync/متصل) + دکمه‌ی کشف خودکار + چیپ‌های ۱۰ هسته + راهنمای گام‌به‌گام (v3) + جستجو/فیلتر + **ردیف‌های چگال با خطوط مویین** روی یک سطح. هر ردیف: نشان هسته، نام، برچسب «کشف‌شده»، مسیر `🇮 host:port → host:port 🌐` با فونت تله‌متری، پیل latency/آفلاین، و سوییچ روشن/خاموش.
2. **شیت کانتکست تونل** (تمام‌صفحه، با Back) — باز می‌شود با ضربه روی هر ردیف: هدر (بازگشت + هسته + نام + کشف‌شده + سوییچ) + **پل دوقطبی** (Iran Relay ← transport ← Foreign Node) + Forwarding + نوار وضعیت (latency/چک + تست) + شبکه‌ی ۴ کاشی اقدام (Script&Docker، ویرایش، تست، حذف) + Auto-Deploy.

**صفر از دست دادن قابلیت** — همه‌چیز ۱:۱ پورت شد:
- حلقه‌ی پس‌زمینه‌ی ۱۵ ثانیه‌ای تست تاخیر + ذخیره در Prefs
- سوییچ → `controlRemoteTunnel(start/stop)` · Auto-Deploy با دیالوگ نتیجه (سرور ایران/خارج) · تست تاخیر با توست · حذف با پاک‌سازی ریموت · کشف خودکار + دیالوگ نتیجه
- **فرم افزودن/ویرایش کامل** (۱۰ هسته، منطق خودکار transport بر پایه‌ی هسته با `LaunchedEffect`، انتخاب سریع سرور، نگاشت پورت‌ها، توکن + مولد + **قانون M17** توکن خالی برای تونل‌های کشف‌شده، تنظیمات اختصاصی BACKPACK/PAQET/NARNIA/SPOOF/BACKHAUL/GOST، سوییچ Auto-Deploy + deploy خودکار هنگام save)
- **ViewTunnelCode** (Iran/Foreign/Docker + TerminalBox + **H3** persist + **H4** نمایش خطای اعتبارسنجی به‌جای کد ساختگی)

### 3-D.2 — پالت فرمان (لایه ۴) · `AeroCommandPalette.kt`
یک سطح جست‌وجوپذیر بالای همه‌چیز، از آیکون چهارم HUD (Tersearch) باز می‌شود:
- **بر هر سرور:** «کاکپیت» (با hint `name · host:port`)
- **بر هر تونل:** تست تاخیر / Auto-Deploy (فقط اگر autoSync) / روشن/خاموش
- سراسری: مدیریت سرورها، تم روشن/تیره، زبان، «پایش دوباره» (requestNow همه‌ی سرورها)
- Back یا ضربه روی scrim می‌بندد؛ سلسله‌مراتب BackHandler درست است (آخرین ثبت‌شده برمی‌گزیند؛ پالت عمیق‌تر از دِک ترکیب می‌شود).
- اقدامات پالت مستقیماً روی state واقعی کار می‌کنند (نقشه، KPIها و صفحه‌ی فلیت همه زنده می‌مانند — `tunnels` در دِک حال‌پذیر شد و فلیت از همان لیست seed می‌گیرد تا ۱۵ثانیه‌ی فلیت روی تغییرات پالت خط نکشد).

### 3-D.3 — منوی رادیال (لایه ۳) · در `AeroCanvasHome.kt`
**بلند-ضربه روی هر گره‌ی نقشه** → منوی رادیال: دکمه‌ی مرکزی ۵۶dp (کاکپیت، accent) + دو سیاره ۴۴dp در ارتفاع ۵۴dp (تست هشدار / پایش دوباره) + scrim بستنده. Back هم منو را می‌بندد (handler عمیق‌تر از handler صفحه‌ی دِک). مختصات رادیال همان قرارداد tap-targetهای گره است (clamp در لبه‌های بوم).

## ۲. فایل‌ها

| فایل | تغییر |
|---|---|
| `AeroTunnelFleet.kt` | جدید — صفحه‌ی فلیت + شیت کانتکست + ۵ دیالوگ |
| `AeroTunnelForm.kt` | جدید — فرم + ViewTunnelCode (پورت ۱:۱) |
| `AeroCommandPalette.kt` | جدید — پالت فرمان + `PaletteCommand` |
| `AeroCanvasHome.kt` | دِک: صفحه‌ی ۱ = فلیت جدید؛ HUD آیکون چهارم؛ state+aqua پالت؛ رادیال نقشه (pointerInput/detectTapGestures)؛ `tunnels` حال‌پذیر + helperهای اقدام |
| `AeroTheme.kt` | کمک‌کننده‌های v3 (`v3Surface`/`v3ListSurface`/`v3ListHeaderRow`/`v3RowDivider`) به‌عنوان primitive مشترک جابه‌جا شدند (قبلاً private در کاکپیت بودند) |
| `TunnelScreen.kt` | **حذف** (۱۶۱ خط) |
| `Strings.kt` | +۶ رشته (fa/en): paletteTitle, paletteHint, refreshNow, radialCockpit, radialTest, radialRefresh |
| `TunnelEngine.kt` | یک ارجاع قدیمی در کامنت (دقت مستندات) |

## ۳. شواهد

| آزمایش | نتیجه |
|---|---|
| کامپایل بسته‌ی JVM (16 فایل، شامل Strings با ۶ فیلد جدید × ۳) | **MAIN OK** |
| جعبه‌ی تست JVM (13 فایل) | **OK (126 tests)** — بدون رجگرسیون |
| `go build` + `go test -race -count=1` (agent) | **OK (1.82s)** |

## ۴. محدودیت‌های صادقانه

- همه‌ی فایل‌های Compose این آیتم **به دلیل نداشتن Android SDK امکان تست واقعی نداشت و فقط از نظر کد بررسی شد**: امضای `TunnelEngine.testTunnel/autoDeployTunnel/controlRemoteTunnel`، فیلدهای `TunnelConfig` (از جمله `lastChecked`، `discovered`)، موجود بودن آیکون‌های material-extended (Tersearch, PlayArrow, Dashboard, CloudSync) و نسخه‌ی `detectTapGestures` در foundation 1.6 یکی‌یکی راستی‌آزمایی شدند.
- دو باگ در پیش‌نویس در review کشف و اصلاح شد: (۱) `manageTick` در اسکوپ دِک نبود → با state محلی حال‌پذیر + seed فلیت حل شد؛ (۲) هندسه‌ی رادیال اشتباه بود → با مکان‌یابی مستقیم (همان قرارداد tap-target) بازنویسی شد.
- نتیجه‌ی Auto-Deploy از **پالت** در دیالوگ فلیت نمایش داده می‌شود (اگر صفحه‌ی فلیت باز نباشد، نتیجه در خودِ Prefs/وضعیت تونل بازتاب دارد؛ اگر بخواهید، می‌توان در 3-E یک snackbar سراسری اضافه کرد).
- RTL نقشه/رادیال در 3-F روی دستگاه ممیزی می‌شود.

## ۵. قدم بعدی (3-E)

سیم‌کشی ابزارها (SFTP/فایل‌ها در کاکپیت؟)، onboarding، و ممیزی parity کامل (مقایسه‌ی سیستماتیک هر قابلیت فاز ۲ با v3).
