# گزارش مرحله‌ی ۱ (مکمل فاز ۲) — یکپارچه‌سازی و دقت‌بخشی به حدس ایران/خارج در discovery

**فایل‌های تغییرکرده:** `TunnelFieldValidation.kt` (تابع جدید)، `TunnelEngine.kt` (۲ نقطه‌ی مصرف) + ۲ تست جدید در `TunnelFieldValidationTest.kt`

## مشکل (ریشه)

وقتی discovery تانلی را روی یک سرور پیدا می‌کرد، باید تصمیم می‌گرفت کدام سمت
«ایران» و کدام «خارج» است. معیار فقط **نام سرور** بود — و آن هم با دو باگ:

1. **دو نسخه‌ی واکنده‌ی inline** از همان حدس (اسکن پروس ۵ کلید: ir/iran/teh/mci/mtn،
   اسکن Docker ۳ کلید: ir/iran/teh) — یک منطق در دو جای کد که از هم افتاده بودند.
2. **false positive واقعی از substring:** `"ir" in "mirror-01"` = true،
   `"ir" in "bird-02"` = true، `"teh" in "technical-7"` = true — سرورهای بی‌ربط
   به‌عنوان «ایران» ثبت می‌شدند و تانل کشف‌شده با نقش معکوس می‌ماند.

## اصلاح

تابع خالص واحد `TunnelFieldValidation.looksLikeIranServer(name)` — **منبع‌وحققت** برای
هر دو اسکن:

- تطبیق **بر پایه‌ی واژه** (تقسیم نام بر غیر الفبا-اعداد)، نه substring سراسری:
  `mirror-01`، `bird-02`، `technical-7`، `iron-01`، `iris-1`، `paris-01`، `mcintosh`
  دیگر **نمی‌افتند**.
- کلیدهای معنادار: `ir`/`ir12` (پیش‌ساز ایران رایج)، `iran*`، `teh`/`tehran*`،
  `mci*`/`mtn*` (اپراتورها) + نام‌های **فارسی** `ایران`/`تهران` (پیش از این هیچ
  فارسی‌ای چک نمی‌شد).
- هر دو اسکن (پروس + Docker) حالا دقیقاً همین تابع را صدا می‌زنند.

**حدود کارکرد (عمدی و مستند):** این تابع فقط **پیش‌فرض** انتساب نقش است؛ کاربر هر
وقت می‌تواند در دیالوگ ویرایش تانل، فیلدهای ایران/خارج را جابه‌جا کند. منبع معتبر
برتر — «نقش صریح در پروفایل سرور» — فعلاً وجود ندارد؛ `ServerConfig` فیلد
region/role ندارد و افزودنش نیاز به تغییر مدل + UI است → **ثبت شد به‌عنوان پیگیری**
(بیشتر در حوزه‌ی فاز ۳ است تا فاز ۲).

## تست

دو تست جدید (JVM) — ۱۶ نام مثبت واقعی (ASCII و فارسی، هر دو اسکن از یک تابع) +
۱۱ نام منفی (همان false positiveهای قبلی + نام‌های خارجی).

**نتیجه:** `MAIN OK` + سوت کامل **`OK (54 tests)`**.

> شفافیت: «انتخاب نقش در discovery» تابعی suspend وابسته به ایجنت است و end-to-end
> اجرا نشد؛ اما منطق حدس (که تمام سطح تصمیم‌گیری این موضوع است) کاملاً خالص و
> JVM-test شده است.

## وضعیت فهرست فاز ۲

با این، **تمام آیتم‌های ثبت‌شده‌ی فاز ۲ بسته شد.** پیگیری‌های بازِ باقی‌مانده:
1. «نقش صریح» در پروفایل سرور (فیلد + UI) — کاندیدای فاز ۳
2. QA نهایی + گزارش مایلی‌استون تجمیعی (مرحله‌ی ۲ برنامه‌ریزی‌شده)
