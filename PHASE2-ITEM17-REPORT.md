# گزارش آیتم ۱۷ فاز ۲ — H20: LocalServer بدون حفاظت روی 0.0.0.0

## یافتهٔ ممیزی — ماهیت واقعی مشکل

ممیزی فاز ۱ درست گزارش داده بود: `LocalHttpServer` روی wildcard bind می‌شد، token/timeout/cap نداشت و پاسخ fallback بدون Content-Length بود. **اما** هنگام پیگیری callerها:

- `LocalHttpServer` و `QrGenerator` (هر دو در `LocalServer.kt`) **صفر ارجاع** در کل اپ دارند — هیچ صفحه‌ای، سرویسی یا تستی از آن‌ها استفاده نمی‌کند
- `git log -S` نشان می‌دهد فایل در همان **اولین commit** ریپازیتوری ساخته شده و هیچ‌وقت به UI وصل نشده → **کد مرده**
- `QrGenerator.generateSimpleBitmap` علاوه بر مرده بودن، **QR جعلی** می‌ساخت (پترن hash-based که اسکن نمی‌شود) — placeholder آشکار

یعنی «آسیب‌پذیری» H20 در عمل دست‌یافتنی نبود؛ خطر واقعی، **نگه‌داشتن یک سرور ناامن ۱۲۶ سطر در کدبیس** بود — اگر روزی به UI وصل می‌شد، دقیقاً همان H20ِ کامل رخ می‌داد (و QR جعلی هم تجربهٔ کاربری را خراب می‌کرد).

## تصمیم (رفع ریشه)

**حذف کامل `LocalServer.kt`** به‌جای تقویت کد مرده:

| گزینه | ارزیابی |
|---|---|
| A) تقویت درجا (token/timeout/cap/expiration) | هزینه بر کدی که کار نمی‌کند + حفظ کد مرده در کدبیس (نقض «no dead code») + توهم اینکه feature وجود دارد |
| **B) حذف** ✓ | سطح خطر شبکه کاملاً از بین می‌رود؛ ارجاع صفر → رگرسیون صفر؛ اگر feature «share محلی» در آینده خواسته شود، **از صفر با طراحی درست** ساخته می‌شود (token یک‌بار + timeout + cap + expiration) |

## تغییرات

| فایل | تغییر |
|---|---|
| `app/src/main/java/org/didban/monitor/LocalServer.kt` | **حذف** (127 سطر: `LocalHttpServer` + `QrGenerator`) |

## شواهد

- `grep` کل `app/` (kt/xml/java): **صفر** ارجاع به `LocalHttpServer`/`QrGenerator`/`getLocalIpAddress` بعد از حذف
- `git log -S "LocalHttpServer" --all`: فقط ۲ hit — commit ساخت + گزارش ممیزی (prose)
- JVM suite: **`OK (136 tests)`** — بدون رگرسیون (این فایل اصلاً در set تست JVM نبود؛ بیلد اندروید اینجا ممکن نیست → سمت اندروید فقط code-review، اما با ارجاع صفر، حذف هیچ symble‌ای را نمی‌شکند)

## صداقت

- این حذف، feature «share فایل/متن از طریق Wi-Fi محلی» را از کدبیس برداشت (هرچند هرگز از UI در دسترس نبود). اگر این feature در فاز ۳ یا بعد خواسته شد، با همین معیارهای H20 از اول ساخته می‌شود.
- بیلد اندروید در این sandbox ممکن نیست؛ ادعای «رگرسیون صفر» بر پایهٔ جستجوی ارجاع در کل in-tree است.

## وضعیت فاز ۲

آیتم‌های ۱ تا ۱۷ تمام. باقی‌مانده از جدول security ممیزی: **M10** (stdout SSH بی‌سقف → OOM دستگاه — رتبه ۱۲)، **H14** (نازلودینگ TLS ساکت در نصب SSH — رتبه ۱۳)، **M17** (KDF/zeroing/auto-lock Vault — رتبه ۱۵) + موارد عملکردی.
